apiVersion: v1
kind: Service
metadata:
  name: java-app-service
  namespace: github-action-canchi
spec:
  type: LoadBalancer
  selector:
    app: java-app
  ports:
    - port: 80
      targetPort: 8080
